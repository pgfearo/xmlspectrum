if (true()) then <div/> else if (false()) then <mod/> else
<abc test="{5 < (:old:) div (:test:) (:new:) and modest = 'abc' and quest='true'}">{
let $ab := function($n as element(new)) {5 + 5}
 return def + kbc[@name < testing ] + count(testing), if (true()) then <!-- abc t -> esting --> else <trial/> 
} testing {{@dev}}
 </abc>
